spring.web.cors.allowed-origins=http://localhost:3000

server.port=${PORT:8081}
ai.service.url=${AI_SERVICE_URL:http://localhost:8000}
spring.application.name=hireiq-backend
```

### Step 9 — Deploy Spring Boot to Railway

1. In Railway click **New Service**
2. Select same `hireiq` GitHub repo
3. Set **Root Directory** to `backend`
4. Add variable:
```
AI_SERVICE_URL=http://hireiq-production-0fda.up.railway.app
```
5. Deploy!

Get your Spring Boot URL:
```
https://hireiq-backend.up.railway.app